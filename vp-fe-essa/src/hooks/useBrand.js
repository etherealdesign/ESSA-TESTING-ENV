/**
 * Re-export useBrand hook for convenience
 * 
 * This allows importing from either:
 * - import { useBrand } from 'contexts/BrandContext'
 * - import { useBrand } from 'hooks/useBrand'
 */

export { useBrand } from '../contexts/BrandContext';
